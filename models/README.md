# Models

Detection weights live in `detection/` and ReID models live in `reid/`. The
binary `.pt` and `.onnx` files are managed by Git LFS.

After cloning, run `git lfs pull` before launching model-backed workflows.
