# Assets

Store documentation images, small demo media, and other repository presentation
assets here. Runtime model weights belong in `models/`; generated output belongs
in the ignored `outputs/` directory.
