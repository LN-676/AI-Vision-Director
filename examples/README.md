# Examples

This directory is reserved for small, runnable examples that demonstrate the
public Python APIs and end-to-end integrations. Product source code and test
fixtures should remain in `src/` and `tests/` respectively.
